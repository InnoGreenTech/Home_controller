void alarme(){

/*    if (temperature+t_offset>t_max||temperature<t_min){bitSet(alarmes_actives,ALARME_T);} // Activation du drapeau d'alarme de température
    else{bitClear(alarmes_actives,ALARME_T);}
    if (humidite+h_offset>h_max||humidite<h_min){bitSet(alarmes_actives,ALARME_H);}   // Activaiton du drapeau d'alarme d'humidité
    else{bitClear(alarmes_actives,ALARME_H);};
    if (niveau+l_offset>l_max||niveau<l_min){bitSet(alarmes_actives,ALARME_L);}      // Activation du drapeau d'alarme de niveau
    else{bitClear(alarmes_actives,ALARME_L);};
    if (compteur1+c_offset>c_max){bitSet(alarmes_actives,ALARME_C);}                //Activation de l'alarme de comptage
    else{bitClear(alarmes_actives,ALARME_C);}; */
  
}
